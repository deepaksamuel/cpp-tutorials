#include <iostream> 

using namespace std; 

int main() 
{ 
    int a=100;
    int b=3;
    float c=1;
    float d=2.4;

    cout<<"The value of a is "<<a<<endl;
    cout<<"The value of b is "<<b<<endl;
    cout<<"The value of c is "<<c<<endl;
    cout<<"The value of d is "<<d<<endl;
    return 0;
}

// The statement int a=1 declares the variable a to be an integer and initializes it to value of 1
// The statement int b=2.4 declares the variable b to be an integer and initializes it to value of 2.4 but since you have declared it to be an integer, the output is an integer
// The statement int c=1 declares the variable c to be a floating point number and initializes it to value of 1.
// The statement int d=2.4 declares the variable d to be a floating point number and initializes it to value of 2.4. Here, since it is declared to be a floating point number, the output is correctly displayed

// assignment:
// a) set float d=2.421212313215313051; and check the output
// b) print out the value of a/b, a+b, a-b and a*b
// c) print out the value of c/d, c+d, c-d and c*d
// d) print out the value of a%b
// e) remove the endl from the cout statements